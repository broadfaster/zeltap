const OPTIONS = ['Any color', 'Red', 'Blue', 'Black', 'Purple']

const PROFESSION_OPTIONS = ['Any profession', 'Doctor', 'Lawyer', 'Chartered Accountant', 'Influencer', 'Others']

const CARD_MATERIAL_OPTIONS = ['Any card', 'PVC', 'Metal Card']

const STEP = 1
const MIN = 1
const MAX = 100

const APP_KEY = 'ZELTAP-user'

export { STEP, MIN, MAX, OPTIONS, APP_KEY, PROFESSION_OPTIONS, CARD_MATERIAL_OPTIONS }
